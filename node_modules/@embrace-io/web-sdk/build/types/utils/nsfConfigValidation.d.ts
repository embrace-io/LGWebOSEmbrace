import type { SDKFeaturesManager } from '../managers/index.js';
import type { SDKInitConfig } from '../sdk/index.js';
import type { DiagLogger } from '@opentelemetry/api';
type ValidateConfigArgs = {
    featureManager: SDKFeaturesManager;
    diag: DiagLogger;
} & Pick<SDKInitConfig, 'propagator' | 'registerGlobally' | 'defaultInstrumentationConfig'>;
export declare const nsfConfigValidation: ({ diag, featureManager, registerGlobally, propagator, defaultInstrumentationConfig, }: ValidateConfigArgs) => boolean;
export {};
//# sourceMappingURL=nsfConfigValidation.d.ts.map