import type { ReadableSpan } from '@opentelemetry/sdk-trace-web';
import { EmbraceProcessor } from '../EmbraceProcessor/index.js';
import type { EmbraceSessionBatchedSpanProcessorArgs } from './types.js';
export declare class EmbraceSessionBatchedSpanProcessor extends EmbraceProcessor {
    private readonly _shutdownOnce;
    private _pendingSpans;
    private readonly _exporter;
    private readonly _limitManager;
    private readonly _spanStorage;
    constructor({ exporter, limitManager, storage, storedSpansExpireTimeoutMS, ...parentArgs }: EmbraceSessionBatchedSpanProcessorArgs);
    forceFlush(): Promise<void>;
    onEnd(span: ReadableSpan): void;
    private _exportSpans;
    onStart(): void;
    getPendingSpansCount(): number;
    storePendingSpans(sessionId: string, sessionSpan: ReadableSpan): void;
    clearStoredSpans(sessionId: string): void;
    shutdown(): Promise<void>;
    private readonly _shutdown;
}
//# sourceMappingURL=EmbraceSessionBatchedSpanProcessor.d.ts.map