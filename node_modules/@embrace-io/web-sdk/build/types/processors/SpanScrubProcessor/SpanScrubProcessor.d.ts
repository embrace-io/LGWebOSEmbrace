import type { ReadableSpan, SpanProcessor } from '@opentelemetry/sdk-trace-web';
import type { SpanScrubProcessorArgs } from './types.js';
export declare class SpanScrubProcessor implements SpanProcessor {
    private readonly _attributeScrubbers;
    constructor({ attributeScrubbers }: SpanScrubProcessorArgs);
    forceFlush(): Promise<void>;
    onEnd(span: ReadableSpan): void;
    onStart(this: void): void;
    shutdown(): Promise<void>;
}
//# sourceMappingURL=SpanScrubProcessor.d.ts.map