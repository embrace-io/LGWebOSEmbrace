import type { ExportResponse, IExporterTransport } from '@opentelemetry/otlp-exporter-base';
import type { PerformanceManager } from '../../utils/index.js';
export declare class RetryingTransport implements IExporterTransport {
    private readonly _transport;
    private readonly _perf;
    constructor(_transport: IExporterTransport, _perf?: PerformanceManager);
    send(data: Uint8Array, timeoutMillis: number): Promise<ExportResponse>;
    shutdown(): void;
    private _retry;
}
//# sourceMappingURL=RetryingTransport.d.ts.map