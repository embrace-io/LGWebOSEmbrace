import type { ExportResponse, IExporterTransport } from '@opentelemetry/otlp-exporter-base';
import type { FetchRequestParameters } from './types.js';
export declare class FetchTransport implements IExporterTransport {
    private readonly _config;
    constructor(_config: FetchRequestParameters);
    private static _compressRequest;
    send(data: Uint8Array, timeoutMillis: number): Promise<ExportResponse>;
    shutdown(): void;
    _asyncSend(data: Uint8Array, timeoutMillis: number): Promise<ExportResponse>;
}
//# sourceMappingURL=FetchTransport.d.ts.map