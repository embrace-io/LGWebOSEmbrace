'use strict';

var otlpExporterBase = require('@opentelemetry/otlp-exporter-base');
var createRetryingTransport = require('../transport/RetryingTransport/createRetryingTransport.cjs');
var createFetchTransport = require('../transport/FetchTransport/createFetchTransport.cjs');

// createOtlpBrowserFetchExportDelegate creates an export delegate that uses
// the Fetch API to send data to an OTLP receiver.
const createOtlpBrowserFetchExportDelegate = (config, serializer)=>// createOtlpNetworkExportDelegate has an internal queue that handles
    // multiple requests going at the same time.
    otlpExporterBase.createOtlpNetworkExportDelegate(config, serializer, createRetryingTransport.createRetryingTransport({
        transport: createFetchTransport.createFetchTransport(config)
    }));

exports.createOtlpBrowserFetchExportDelegate = createOtlpBrowserFetchExportDelegate;
//# sourceMappingURL=otlpBrowserFetchExportDelegate.cjs.map
