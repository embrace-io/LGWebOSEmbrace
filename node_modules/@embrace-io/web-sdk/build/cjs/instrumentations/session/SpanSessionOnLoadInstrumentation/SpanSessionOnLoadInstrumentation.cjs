'use strict';

var EmbraceInstrumentationBase = require('../../EmbraceInstrumentationBase/EmbraceInstrumentationBase.cjs');

class SpanSessionOnLoadInstrumentation extends EmbraceInstrumentationBase.EmbraceInstrumentationBase {
    constructor({ diag } = {}){
        super({
            instrumentationName: 'SpanSessionOnLoadInstrumentation',
            instrumentationVersion: '1.0.0',
            diag,
            config: {}
        });
        if (this._config.enabled) {
            this.enable();
        }
    }
    disable() {
        this.sessionManager.endSessionSpanInternal('unknown');
    }
    enable() {
        this.sessionManager.startSessionSpan({
            reason: 'init'
        });
    }
}

exports.SpanSessionOnLoadInstrumentation = SpanSessionOnLoadInstrumentation;
//# sourceMappingURL=SpanSessionOnLoadInstrumentation.cjs.map
