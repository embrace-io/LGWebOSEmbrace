'use strict';

/**
 * Remove multiple event listeners from a target element.
 * example:
 *
 * ```
 * bulkRemoveEventListener({
 *  target: window,
 *  events: ['visibilitychange', 'resize'],
 *  callback: someRefToACallbackFunction,
 *  });
 *```
 * */ const bulkRemoveEventListener = ({ target, events, callback })=>{
    events.forEach((event)=>{
        target.removeEventListener(event, callback);
    });
};

exports.bulkRemoveEventListener = bulkRemoveEventListener;
//# sourceMappingURL=bulkRemoveEventListener.cjs.map
