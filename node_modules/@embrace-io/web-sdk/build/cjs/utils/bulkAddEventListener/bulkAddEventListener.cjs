'use strict';

/**
 * Add multiple event listeners to a target element.
 * example:
 *
 * ```
 * bulkAddEventListener({
 *  target: window,
 *  events: ['visibilitychange', 'resize'],
 *  callback: () => console.log('Event fired'),
 *  });
 *```
 * */ const bulkAddEventListener = ({ target, events, callback })=>{
    events.forEach((event)=>{
        target.addEventListener(event, callback);
    });
};

exports.bulkAddEventListener = bulkAddEventListener;
//# sourceMappingURL=bulkAddEventListener.cjs.map
