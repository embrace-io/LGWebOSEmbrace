'use strict';

var NoOpTraceManager = require('../NoOpTraceManager/NoOpTraceManager.cjs');

const NOOP_TRACE_MANAGER = new NoOpTraceManager.NoOpTraceManager();
class ProxyTraceManager {
    getDelegate() {
        return this._delegate ?? NOOP_TRACE_MANAGER;
    }
    setDelegate(delegate) {
        this._delegate = delegate;
    }
    startSpan(name, options, context) {
        return this.getDelegate().startSpan(name, options, context);
    }
    setSpan(context, span) {
        return this.getDelegate().setSpan(context, span);
    }
    getSpan(context) {
        return this.getDelegate().getSpan(context);
    }
}

exports.ProxyTraceManager = ProxyTraceManager;
//# sourceMappingURL=ProxyTraceManager.cjs.map
