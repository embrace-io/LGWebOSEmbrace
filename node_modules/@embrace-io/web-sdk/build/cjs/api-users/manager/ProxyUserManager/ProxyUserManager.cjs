'use strict';

var NoOpUserManager = require('../NoOpUserManager/NoOpUserManager.cjs');

const NOOP_USER_MANAGER = new NoOpUserManager.NoOpUserManager();
class ProxyUserManager {
    getDelegate() {
        return this._delegate ?? NOOP_USER_MANAGER;
    }
    setDelegate(delegate) {
        this._delegate = delegate;
    }
    getEmbraceUserId() {
        return this.getDelegate().getEmbraceUserId();
    }
    getUserId() {
        return this.getDelegate().getUserId();
    }
    setUserId(userId) {
        this.getDelegate().setUserId(userId);
    }
    clearUserId() {
        this.getDelegate().clearUserId();
    }
}

exports.ProxyUserManager = ProxyUserManager;
//# sourceMappingURL=ProxyUserManager.cjs.map
