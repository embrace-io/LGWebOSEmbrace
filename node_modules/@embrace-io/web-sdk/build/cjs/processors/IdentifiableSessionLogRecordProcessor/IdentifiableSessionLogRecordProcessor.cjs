'use strict';

var incubating = require('@opentelemetry/semantic-conventions/incubating');
var generateUUID = require('../../utils/generateUUID.cjs');

class IdentifiableSessionLogRecordProcessor {
    constructor({ spanSessionManager }){
        this._spanSessionManager = spanSessionManager;
    }
    // no-op
    forceFlush() {
        return Promise.resolve(undefined);
    }
    onEmit(logRecord) {
        logRecord.setAttributes({
            [incubating.ATTR_LOG_RECORD_UID]: generateUUID.generateUUID(),
            [incubating.ATTR_SESSION_ID]: this._spanSessionManager.getSessionId()
        });
    }
    // no-op
    shutdown() {
        return Promise.resolve(undefined);
    }
}

exports.IdentifiableSessionLogRecordProcessor = IdentifiableSessionLogRecordProcessor;
//# sourceMappingURL=IdentifiableSessionLogRecordProcessor.cjs.map
