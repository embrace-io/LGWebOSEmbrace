'use strict';

var semanticConventions = require('@opentelemetry/semantic-conventions');
var attributes = require('../../constants/attributes.cjs');

class EmbraceLogRecordProcessor {
    constructor({ urlDocument = window.document } = {}){
        this._urlDocument = urlDocument;
    }
    // no-op
    forceFlush() {
        return Promise.resolve(undefined);
    }
    onEmit(logRecord) {
        if (!logRecord.attributes[attributes.KEY_EMB_TYPE]) {
            logRecord.setAttribute(attributes.KEY_EMB_TYPE, attributes.EMB_TYPES.SystemLog);
        }
        logRecord.setAttribute(semanticConventions.ATTR_URL_FULL, this._urlDocument.URL);
    }
    // no-op
    shutdown() {
        return Promise.resolve(undefined);
    }
}

exports.EmbraceLogRecordProcessor = EmbraceLogRecordProcessor;
//# sourceMappingURL=EmbraceLogRecordProcessor.cjs.map
