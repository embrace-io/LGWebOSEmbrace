'use strict';

var NoOpLogManager = require('../NoOpLogManager/NoOpLogManager.cjs');

const NOOP_LOG_MANAGER = new NoOpLogManager.NoOpLogManager();
class ProxyLogManager {
    getDelegate() {
        return this._delegate ?? NOOP_LOG_MANAGER;
    }
    setDelegate(delegate) {
        this._delegate = delegate;
    }
    logException(error, options) {
        this.getDelegate().logException(error, options);
    }
    message(message, level, options) {
        this.getDelegate().message(message, level, options);
    }
}

exports.ProxyLogManager = ProxyLogManager;
//# sourceMappingURL=ProxyLogManager.cjs.map
