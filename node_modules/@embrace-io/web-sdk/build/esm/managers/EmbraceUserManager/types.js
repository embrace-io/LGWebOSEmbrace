import { KEY_ENDUSER_PSEUDO_ID } from '../../api-users/manager/constants/index.js';

const isUser = (user)=>typeof user[KEY_ENDUSER_PSEUDO_ID] === 'string' && user[KEY_ENDUSER_PSEUDO_ID].length === 32;
const isUserId = (userId)=>typeof userId === 'string' && userId.length === 32;

export { isUser, isUserId };
//# sourceMappingURL=types.js.map
