import { diag } from '@opentelemetry/api';

class EmbraceProcessor {
    constructor({ diag: providedDiag, processorName }){
        this._processorName = processorName;
        this._diag = providedDiag ?? diag.createComponentLogger({
            namespace: processorName
        });
    }
    /* Returns the diag logger */ get diag() {
        return this._diag;
    }
    /* Returns the processor name */ get processorName() {
        return this._processorName;
    }
}

export { EmbraceProcessor };
//# sourceMappingURL=EmbraceProcessor.js.map
