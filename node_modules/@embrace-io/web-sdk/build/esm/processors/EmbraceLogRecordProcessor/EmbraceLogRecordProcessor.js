import { ATTR_URL_FULL } from '@opentelemetry/semantic-conventions';
import { KEY_EMB_TYPE, EMB_TYPES } from '../../constants/attributes.js';

class EmbraceLogRecordProcessor {
    constructor({ urlDocument = window.document } = {}){
        this._urlDocument = urlDocument;
    }
    // no-op
    forceFlush() {
        return Promise.resolve(undefined);
    }
    onEmit(logRecord) {
        if (!logRecord.attributes[KEY_EMB_TYPE]) {
            logRecord.setAttribute(KEY_EMB_TYPE, EMB_TYPES.SystemLog);
        }
        logRecord.setAttribute(ATTR_URL_FULL, this._urlDocument.URL);
    }
    // no-op
    shutdown() {
        return Promise.resolve(undefined);
    }
}

export { EmbraceLogRecordProcessor };
//# sourceMappingURL=EmbraceLogRecordProcessor.js.map
