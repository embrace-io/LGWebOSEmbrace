import { createOtlpNetworkExportDelegate } from '@opentelemetry/otlp-exporter-base';
import { createRetryingTransport } from '../transport/RetryingTransport/createRetryingTransport.js';
import { createFetchTransport } from '../transport/FetchTransport/createFetchTransport.js';

// createOtlpBrowserFetchExportDelegate creates an export delegate that uses
// the Fetch API to send data to an OTLP receiver.
const createOtlpBrowserFetchExportDelegate = (config, serializer)=>// createOtlpNetworkExportDelegate has an internal queue that handles
    // multiple requests going at the same time.
    createOtlpNetworkExportDelegate(config, serializer, createRetryingTransport({
        transport: createFetchTransport(config)
    }));

export { createOtlpBrowserFetchExportDelegate };
//# sourceMappingURL=otlpBrowserFetchExportDelegate.js.map
