import { RetryingTransport } from './RetryingTransport.js';
import { OTelPerformanceManager } from '../../utils/PerformanceManager/OTelPerformanceManager.js';

/**
 * Creates an Exporter Transport that retries on 'retryable' response.
 */ const createRetryingTransport = (options)=>new RetryingTransport(options.transport, options.perf ?? new OTelPerformanceManager());

export { createRetryingTransport };
//# sourceMappingURL=createRetryingTransport.js.map
