{
  "name": "lg-webos-sample",
  "version": "1.0.0",
  "description": "Minimal LG webOS sample app",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "npx http-server -p 8080 -c10",
    "package": "npx ares-package .",
    "deploy": "bash ./scripts/deploy.sh"
  },
  "devDependencies": {
    "@webos-tools/cli": "3.2.1"
  }
}
